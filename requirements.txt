# Install/upgrade to the newest compatible set:
#   uv pip install -r requirements.txt -U
# Freeze (pin) once the resolved set is verified on the GPU box:
#   uv pip freeze > requirements.lock

# Core training
torch
transformers
trl
peft
accelerate
datasets
bitsandbytes

# Eval
lm-eval
# trl 1.13.0 supports vLLM 0.19.1-0.28.0; 0.29.0 triggers compatibility warnings.
vllm>=0.19.1,<0.29

# Tracking & analysis
wandb
pandas
numpy
matplotlib
seaborn
scipy
scikit-learn

# Utils
python-dotenv
tqdm
rich
